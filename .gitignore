# Windows 下载残留元数据
*.Zone.Identifier

# 可再生发布包（不要把版本压缩包堆进源码仓库）
*.zip
/.zip

# 本机 Agent / IDE 运行状态；保留在本地，不参与协作
/.agents/
/.claude/
/.superpowers/
/skills-lock.json

# 误将仓库根目录或 .trellis 目录导入微信开发者工具时生成的配置
# 正确项目配置位于 miniapp 源码构建后的 dist/*/mp-weixin 中。
/project.config.json
/project.private.config.json
/.trellis/project.config.json
/.trellis/project.private.config.json

# CloudBase CLI 本地部署配置（含环境标识 envId 与服务名，不便公开）
cloudbaserc.json

# 微信开发者工具私有配置（任意被导入的目录都会生成）
project.private.config.json

# 本机 IDE / Agent 运行状态；保留在本地，不参与协作
/.codebuddy/
